<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Stars" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="CustomRatingBar" parent="@android:style/Widget.Material.RatingBar.Indicator">
        <item name="android:progressTint">@color/gold</item>  <!-- Filled stars color -->
        <item name="android:progressBackgroundTint">@color/gray</item>  <!-- Empty stars color -->
        <item name="android:secondaryProgressTint">@color/light_gold</item>  <!-- Half-filled stars -->
    </style>
</resources>